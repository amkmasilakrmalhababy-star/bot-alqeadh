{
  "name": "bot-alqeadh",
  "version": "1.0.0",
  "description": "ALQEADH Download Bot",
  "main": "bot.js",
  "scripts": {
    "start": "node bot.js",
    "postinstall": "apt-get update && apt-get install -y ffmpeg && pip install yt-dlp"
  },
  "dependencies": {
    "node-telegram-bot-api": "^0.61.0",
    "axios": "^1.6.0",
    "fs-extra": "^11.2.0"
  }
}
